# Keep command output in terminal after closing
LESS='-R --no-init --quit-if-one-screen'

# Always grep with color
alias grep='grep --color=auto'

#
# git
#
alias undo_commit_save_changes='git reset "HEAD^"'
